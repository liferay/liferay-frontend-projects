# Make `yarn version` produce the right commit message and tag for this package.
version-tag-prefix "jest-junit-reporter/v"
version-git-message "chore: prepare @liferay/jest-junit-reporter v%s release"
