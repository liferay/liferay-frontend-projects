// Require webpack bundles generated by the adapted framework's build
<%= requireBlock %>

// Load one-time modules
<%= preloadBlock %>

// Require our adapt runtime support module
var _ADAPT_RT_ = require("./adapt-rt"); 

// Invoke framework's entry point passing Liferay arguments
module.exports = function(_LIFERAY_PARAMS_) {
  <%= entryPointFunction %>(_LIFERAY_PARAMS_, _ADAPT_RT_);
};