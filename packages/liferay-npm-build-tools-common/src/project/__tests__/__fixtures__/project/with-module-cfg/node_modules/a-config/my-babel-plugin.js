module.exports = function() {
	// Standard babel plugins don't return values, but because we are calling
	// it from a test, we just return a string to make sure that we are really
	// calling what we want.
	return 'Hi from babel plugin!';
};
