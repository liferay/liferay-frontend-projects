import { Component } from '@angular/core';

import LiferayParams from '../types/LiferayParams'

declare const Liferay: any;

@Component({
	templateUrl: '/o/<%= pkgJson.name %>/app/app.component.html'
})
export class AppComponent {
	params: LiferayParams;
	labels: any;

	constructor() {
		this.labels = {        
			<% if (hasSettings) { %>
			settings: <%- labels.settings %>,
			<% } %>
			<% if (hasPreferences) { %>
			preferences: <%- labels.preferences %>,
			<% } %>
			portletNamespace: <%- labels.portletNamespace %>,
        	contextPath: <%- labels.contextPath %>,
			portletElementId: <%- labels.portletElementId %>,
		}
	}

	get settingsJSON() {
		return JSON.stringify(this.params.settings, null, 2);
	}

	get preferencesJSON() {
		return JSON.stringify(this.params.preferences, null, 2);
	}
}
