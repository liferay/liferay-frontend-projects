{
	"name": "<%= themeDirName %>",
	"version": "<%= packageVersion %>",
	"keywords": [
		"liferay-theme"
	],
	"liferayTheme": {
		"themelet": true,
		"version": "<%= liferayVersion %>"
	}<% if (publishTag) { %>,
	"publishConfig": {
		"tag": "<%= publishTag %>"
	}<% } %>
}