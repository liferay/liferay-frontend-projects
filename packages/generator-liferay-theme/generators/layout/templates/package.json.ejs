{
	"name": "<%= layoutDirName %>",
	"version": "1.0.0",
	"main": "package.json",
	"keywords": ["liferay-theme"],
	"liferayLayoutTemplate": {
		"version": "<%= liferayVersion %>"
	},
	"devDependencies": <%- devDependencies %>,
	"scripts": {
		"init": "gulp init",
		"build": "gulp build",
		"deploy": "gulp deploy",
		"init": "gulp init"
	}
}
